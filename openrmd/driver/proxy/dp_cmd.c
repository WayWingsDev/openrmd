#include "dp.h"
#include "dp_cmd.h"
#include "dp_protocol.h"

int cmd_process(int sock, char *buff)
{
//	if (sscanf(buff, "%[^-]-%d-%d-%d", plat, &ptz_info->device_type,
//			&ptz_info->device_id, &ptz_info->channel_id) != 4)
	return 0;
}
